{
  "name": "organizador-postech",
  "version": "0.1.0",
  "description": "Organizador de Arquivos POSTECH - FIAP",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build:win": "electron-builder --win --x64 --publish never",
    "build:mac": "electron-builder --mac --x64 --publish never",
    "build:all": "electron-builder --win --mac --x64 --publish never",
    "release:dev": "electron-builder --win --x64 --publish always --preRelease",
    "release:win": "electron-builder --win --x64 --publish always"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/DyFuchs/Ferramentas_POSTECH_Electron.git"
  },
  "build": {
    "appId": "com.postech.organizador",
    "productName": "Organizador POSTECH",
    "directories": {
      "output": "dist"
    },
    "files": [
      "main.js",
      "preload.js",
      "src/**/*",
      "assets/**/*"
    ],
    "extraResources": [
      {
        "from": "rust-backend/target/release/organizador-postech-backend.exe",
        "to": "backend/organizador-postech-backend.exe"
      }
    ],
    "win": {
      "target": "nsis",
      "icon": "assets/icon.ico",
      "publish": [
        {
          "provider": "github",
          "owner": "DyFuchs",
          "repo": "Ferramentas_POSTECH_Electron",
          "releaseType": "release"
        }
      ]
    },
    "mac": {
      "target": "dmg",
      "icon": "assets/icon.icns",
      "publish": [
        {
          "provider": "github",
          "owner": "DyFuchs",
          "repo": "Ferramentas_POSTECH_Electron",
          "releaseType": "release"
        }
      ]
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true
    }
  },
  "devDependencies": {
    "electron": "^33.0.0",
    "electron-builder": "^25.0.0",
    "electron-updater": "^6.3.0"
  },
  "dependencies": {
    "electron-updater": "^6.8.9"
  }
}
